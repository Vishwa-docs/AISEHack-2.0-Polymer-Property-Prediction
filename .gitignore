# OS cruft
.DS_Store
Thumbs.db

# Python cruft
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# Large run artifacts (keep per-experiment decisions/metrics in git, not predictions blobs)
*.npz
*.npy
*.pkl
*.joblib
*.parquet
experiments/**/oof_predictions.csv
experiments/**/predictions.csv
experiments/**/*.zip

# Secrets
.env
kaggle.json

Oracle/

# --- consolidation delivery ---
# The two publishable repositories are their own git repos (see RUN.md step 10).
/Personal/
/AISEHack-2.0-Sandman-Polymer-Property-Prediction-Codebase/
# Held-out verification data: never committed.
Consolidation/06_oracle_QUARANTINE/
# Build staging
.build/
