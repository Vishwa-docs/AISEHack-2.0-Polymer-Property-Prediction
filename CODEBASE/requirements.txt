# Environment validated for this pipeline.
# The V57 spine's ei/eea leaf models (MLPRegressor, GaussianProcessRegressor, rdEHT,
# RDKit Descriptors3D) are version-sensitive: below scikit-learn 1.9.0 the ei target
# collapses (0.871 -> 0.512). Pin these versions to reproduce the verified 0.90352.
python==3.11.7
rdkit==2026.03.5
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
lightgbm==4.7.0
xgboost==3.2.0
joblib
